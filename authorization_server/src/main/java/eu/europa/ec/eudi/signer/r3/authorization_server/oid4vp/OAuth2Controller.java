package eu.europa.ec.eudi.signer.r3.authorization_server.oid4vp;

import eu.europa.ec.eudi.signer.r3.authorization_server.oid4vp.dto.*;
/*
@RestController
@RequestMapping(value = "/csc/v2/oauth2")
public class OAuth2Controller {
    @Autowired
    private OID4VPService oid4VPService;

    // returns HTTP 302 Found Location ...
    @GetMapping(value = "/authorize", consumes = "application/x-www-form-urlencoded", produces = "application/x-www-form-urlencoded")
    public OAuth2AuthorizeResponse authorize(@RequestBody OAuth2AuthorizeRequest authorizeRequestDTO) {
        System.out.println(authorizeRequestDTO.toString());



         * if(authorizeRequestDTO.getScope().equals("service")){
         * // service authentication using OID4VP
         * oid4VPService.serviceAuthenticationWithOID4VP();
         * }
         * else if (authorizeRequestDTO.getScope().equals("credential")){
         * // credential authorization using OID4VP
         * oid4VPService.credentialAuthorizationWithOID4VP();
         * }
         * else if(authorizeRequestDTO.getAuthorization_details().equals("credential")){
         * // credential authorization using OID4VP
         * oid4VPService.credentialAuthorizationWithOID4VP();
         * }


        return new OAuth2AuthorizeResponse();
    }

    @PostMapping(value = "/token", consumes = "application/x-www-form-urlencoded", produces = "application/json")
    public OAuth2TokenResponse token(@RequestBody OAuth2TokenRequest tokenRequestDTO) {
        System.out.println(tokenRequestDTO.toString());
        return new OAuth2TokenResponse();
    }

}*/
